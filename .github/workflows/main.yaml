name: CD
on: 
  push:
    branches: [preprod]

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SSHPASS: ${{ secrets.APP_PASS }}

    steps:
      - uses: actions/checkout@v2
          
      - name: APT UPDATE and INSTALL SSHPASS
        run:  
          sshpass -p 'qPRrVHChBa8c9g' ssh -o stricthostkeychecking=no -p 5022 ${{ secrets.APP_USER }}@${{ secrets.APP_HOST }}
          "cd tatanimo.mds-lille.yt/ &&
          git pull origin preprod &&
          composer install &&
          npm install &&
          npm run build &&
          php bin/console d:m:m -n"
