{% extends 'base.html.twig' %}

{% block title %}ServicesType{% endblock %}

{% block body %}
    <h1>ServicesType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ services_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ services_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_services_type_index') }}">back to list</a>

    <a href="{{ path('app_services_type_edit', {'id': services_type.id}) }}">edit</a>

    {{ include('services_type/_delete_form.html.twig') }}
{% endblock %}
